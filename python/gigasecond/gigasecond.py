from datetime import datetime
from datetime import timedelta


def add(moment):
    return moment + timedelta(seconds=10**9)

# Just needed to learn about the datetime library from <https://docs.python.org/3/library/datetime.html#timedelta-objects>
